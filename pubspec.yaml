name: responsive_grid
description: Responsive Grid Layout and List for Flutter
version: 2.4.4
homepage: https://github.com/iamtoricool/ResponsiveGrid_Flutter

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
